// Count to ten!

// We need to specify...

// Where should we START counting?
let start = 1
// Where is the FINISH line?
let finish = 10
// What's the STEP SIZE we should use?
let step = 1

//    START           FINISH       STEP SIZE
for ( let count = start;  count < finish;  count += step )  {
    
    console.log(count)

}
